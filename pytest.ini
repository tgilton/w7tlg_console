[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    browser: needs node + Chrome, loads dashboard/console.html for real (~90s).
             Run the fast suite with:  pytest -m "not browser"
